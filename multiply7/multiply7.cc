/**
 * Universidad de La Laguna
 * Escuela Superior de Ingeniería y Tecnología
 * Grado en Ingeniería Informática
 * Informática Básica
 *
 * @author Godgith John
 * @date Nov 28, 2022
 * @brief find all the multiples of 7 that can be formed with the n digits.
 * https://www.geeksforgeeks.org/problem-in-comparing-floating-point-numbers-and-how-to-compare-them-correctly/
 * @see https://jutge.org/problems/P11875
 */

#include <iostream>
#include <vector>

std::vector<int> Multiple(std::vector<int> numeros, int capacidad ) {
  
}

int main() {
  int capacidad{0}, contenido;
  std::vector<int> numeros;
  for (int i{0}; i < capacidad; i++) {
    std::cin >> contenido;
    numeros.push_back(contenido);
  } 
  return 0;
}